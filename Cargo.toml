[package]
name = "pqguard"
version = "0.1.0"
edition = "2021"
description = "Post-quantum file encryption CLI — ML-KEM-768 + AES-256-GCM"
license = "MIT"
repository = "https://github.com/BartoszOsiej/pqguard"
readme = "README.md"
keywords = ["post-quantum", "cryptography", "ml-kem", "encryption", "cli"]
categories = ["command-line-utilities", "cryptography"]

[[bin]]
name = "pqguard"
path = "src/main.rs"

[dependencies]
# CLI
clap = { version = "4.6", features = ["derive"] }
# Post-quantum KEM (ML-KEM-768 / Kyber768) — RustCrypto standard
ml-kem = { version = "0.3", features = ["getrandom"] }
# Key derivation
hkdf = "0.13"
sha2 = "0.11"
# Symmetric encryption
aes-gcm = "0.11"
# Random number generation
rand = "0.10"
getrandom = "0.4"
# Encoding
base64 = "0.23"
# Errors
anyhow = "1"
# Colors
colored = "3.1"

[dev-dependencies]
tempfile = "3.27"
criterion = { version = "0.5", features = ["html_reports"] }
proptest = "1.11"

[[bench]]
name = "pqguard_bench"
harness = false

[profile.release]
lto = true
codegen-units = 1
strip = true
